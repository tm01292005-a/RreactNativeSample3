import React, {useState, useEffect} from 'react';
import {Button, Layout} from '@ui-kitten/components';

const Test = (props) => {
  return (
    <Layout>
      <Button size="large">
        ログアウト
      </Button>
    </Layout>
  );
};

export default Test;
